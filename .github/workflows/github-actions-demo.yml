name: "Newman Tests"
on: [push, pull_request]

jobs:
build:
runs-on: ubuntu-latest
container:
image:postman/newman
steps:
-name: Checkout
users: actions/checkout@v2

-name: Run APU tests
run: mewman run RestfulBookerBVT.postman_collection.json -e Production.postman_enviroment.json
